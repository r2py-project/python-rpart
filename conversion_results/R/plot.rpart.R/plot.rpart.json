{
  "imports": [
    "import numpy as np",
    "import matplotlib.pyplot as plt",
    "import matplotlib.axes",
    "from typing import Any"
  ],
  "function_prototype": "def plot_rpart(x: dict[str, Any], uniform: bool = False, branch: float = 1, compress: bool = False, nspace: float | None = None, margin: float = 0, minbranch: float = 0.3, branch_col: int | str = 1, branch_lty: int = 1, branch_lwd: float = 1, ax: matplotlib.axes.Axes | None = None, **kwargs: Any) -> dict[str, np.ndarray[Any, np.dtype[np.float64]]]:",
  "function_body": [
    "    global rpart_env",
    "    if not isinstance(x, dict) or 'frame' not in x:",
    "        raise TypeError('Not a legitimate \"rpart\" object')",
    "    if len(x['frame']) <= 1:",
    "        raise ValueError('fit is not a tree, just a root')",
    "",
    "    if compress and nspace is None:",
    "        nspace = branch",
    "    if not compress:",
    "        nspace = -1",
    "",
    "    parms: dict[str, Any] = {",
    "        'uniform': uniform,",
    "        'branch': branch,",
    "        'nspace': nspace,",
    "        'minbranch': minbranch,",
    "    }",
    "",
    "    temp = rpartco(x, parms)",
    "    xx = temp['x']",
    "    yy = temp['y']",
    "",
    "    temp1 = [np.min(xx) - np.ptp(xx) * margin, np.max(xx) + np.ptp(xx) * margin]",
    "    temp2 = [np.min(yy) - np.ptp(yy) * margin, np.max(yy) + np.ptp(yy) * margin]",
    "",
    "    if ax is None:",
    "        _, ax = plt.subplots()",
    "    ax.set_xlim(temp1[0], temp1[1])",
    "    ax.set_ylim(temp2[0], temp2[1])",
    "    ax.set_axis_off()",
    "",
    "    rpart_env[f'device{1}'] = parms",
    "",
    "    node = x['frame'].index.astype(float).values",
    "    temp = rpart_branch(xx, yy, node, branch)",
    "",
    "    if branch > 0:",
    "        ax.text(xx[0], yy[0], '|', ha='center', va='bottom')",
    "",
    "    col_map: dict[int, str] = {0: 'white', 1: 'black', 2: 'red', 3: '#00CD00', 4: 'blue'}",
    "    lty_map: dict[int, str] = {1: '-', 2: '--', 3: ':', 4: '-.'}",
    "    color: str = col_map.get(branch_col, branch_col) if isinstance(branch_col, int) else branch_col",
    "    linestyle: str = lty_map.get(branch_lty, '-')",
    "",
    "    ax.plot(",
    "        temp['x'].ravel(order='F'),",
    "        temp['y'].ravel(order='F'),",
    "        color=color,",
    "        linestyle=linestyle,",
    "        linewidth=branch_lwd,",
    "    )",
    "",
    "    return {'x': xx, 'y': yy}"
  ]
}
